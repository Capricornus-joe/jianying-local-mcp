# Changelog

记录已实现的行为。程序测试、MCP 协议验证和剪映原生导出分别说明，不将样片结果推广为所有版本兼容。

## 未发布

### 修复 — 2026-09-18

- 补齐全部 22 个工具的 `idempotentHint`：8 个只读工具设为 `true`，14 个可能写入工程或媒体的工具设为 `false`。四项行为标记均显式给出，不改变工具功能或权限。
- 增加真实 stdio 连接检查，确认客户端收到的每个工具都包含四项布尔标记。

## 0.3.0 — 2026-09-10

### 新增

- `batch_edit`：1–100 个有序编辑步骤在内存组合，成功后只保存一份新修订，不生成中间工程。支持 edit、features、motion、subtitles、style_subtitles、reorder、ripple_delete；任一步骤失败则在写入前终止。
- 批处理返回 `review_scope`，提示变动片段、时间范围、可能受影响的音频及视觉轨道顺序。此字段不执行原生验收。
- 媒体元数据进程内缓存：2048 项、五分钟 TTL，按媒体文件与 FFmpeg 身份失效，合并重复并发探测，不缓存失败。
- 合成输入的三轮真实 stdio 基准与响应/分页测试。工具数量由 21 增至 22。

### 行为变化

- 11 个返回计划的写入工具默认省略完整 `plan`，可用 `include_plan=true` 恢复完整返回。底层 `ProjectStore` 的返回不变。
- `read_managed_project` 默认返回摘要；`view="clips"` 分页，单页最多 200 项，可按轨道过滤；`view="full"` 明确读取完整计划。
- 依赖旧版完整响应的 MCP 客户端需要显式选择上述全文参数。

### 验证

- 记录的完整回归：375 项测试、60 个子测试通过。
- 三轮基准均确认批量与逐步操作的最终片段语义等价，失败批次无写入。所测执行时间不含剪映启动、渲染或导出；详见[性能说明](docs/performance.md)。
- 没有新增 Mac 自动导出或加密草稿支持，也没有宣称 0.3 接口已另做完整原生导出验收。

## 0.2.1 — 2026-09-09

- 为视频、图片和音频片段增加 `display_name`，在管理计划、修订和原生素材显示字段中保留；长度 1–120 字符，拒绝无效或控制字符。
- 同源片段可使用不同显示名和独立材质记录，媒体文件仍只复制一次。文字片段继续以正文显示。
- 工具数量保持 21。记录的完整程序回归为 307 项测试、60 个子测试通过；名称在具体剪映版本中的显示仍需原生检查。

## 0.2.0 — 2026-09-09

- 将工具扩充到 21 个：加入高级片段属性、九种线性运动预设、轨道重排、波纹删除、批量字幕样式、SRT 导出、顺序拼接、本地媒体预处理、抽帧和缓存资源查询/应用。
- 支持视觉变换、裁切、镜像、文字描边/背景/阴影、线性关键帧、音频淡入淡出、原生蒙版及动画/转场描述。
- 本地调色、倒放、音频提取/归一化/降噪生成新素材，不覆盖源文件；这些处理不会成为可逐项开关的原生调整参数。
- 修正 Mac 蒙版记录与启用字段、同源不同裁切的材质隔离、文字样式的 UTF-16 范围、线性关键帧拆分边界和归一化后的音频时间信息。
- 记录的回归为 290 项测试、49 个子测试通过；另有 Mac 剪映专业版 11.4.0 的 16 秒演示草稿打开与 GUI 导出验证。效果覆盖范围见[兼容性说明](docs/compatibility.md)。

## 0.1.0 — 2026-09-09

- 提供本地 stdio MCP 服务、环境与媒体检查、明文原生草稿检查、管理工程列表与读取。
- 从计划创建自包含草稿，支持视频、图片、音频和原生文字轨，以及追加、更新、移除、拆分和 SRT 导入。
- 使用独立名称创建修订，保留源管理工程；将新工程登记至剪映首页时备份索引并保留既有登记。
- 通过真实 MCP 子进程调用与程序测试；历史 6 秒样片可在 Mac 剪映中打开、修改中文字幕并由 GUI 导出。
- 原生导出由桌面界面完成；服务未提供自动导出、加密草稿编辑或 GUI 修改同步。
