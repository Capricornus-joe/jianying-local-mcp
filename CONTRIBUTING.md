# 参与开发

欢迎提交可复现的问题、文档修正和范围明确的改进。本项目是独立的本地草稿工具，不是剪映官方 SDK；原生兼容性需要在具体应用版本中验证。

## 本地开发

使用 macOS 和 Python 3.11 或 3.12，在仓库根目录运行：

```sh
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[test]'
.venv/bin/python -m pytest -q
```

测试使用临时目录和合成媒体。标准测试不要求安装、登录或打开剪映，也不需要实际效果缓存、私人素材或用户草稿。FFmpeg 默认由 `imageio-ffmpeg` 提供；需要测试指定版本时，可设置 `JIANYING_FFMPEG` 为本地可执行文件的绝对路径。

示例脚本会生成合成素材和托管工程，属于可选的人工演示，不必为每次改动重复运行。`--with-cached-effects` 仅用于已有相应本机缓存的原生实验，不属于无外部资源的标准测试。

## 提交改动

- 说明具体问题、最终行为和验证范围。性能优化同时报告运行时间、工具调用次数、输入规模和测量环境，避免用小样本推导整体剪片速度。
- 按改动范围运行相关测试；提交前运行一次完整测试。不要把 JSON 结构校验或 CI 通过写成“剪映预览与导出通过”。原生验证应另记应用版本、操作、导出证据和未覆盖范围。
- 保持 `dry_run` 不写文件、素材为本地显式输入、修订使用新项目名、发布不覆盖已有项目等约定。涉及路径、复制、并发或发布逻辑时，补充失败路径的回归测试。
- 问题和测试使用合成素材。不要提交真实用户目录、账号、令牌、私人字幕、完整草稿、官方效果包、字体文件或生产视频。提交前检查 `git diff --cached`；`.gitignore` 不会移除已经被追踪的内容。
- 添加第三方内容时保留来源和许可证，并更新 `THIRD_PARTY_NOTICES.md`。只包含有权分发的内容。

CI 在 macOS 的 Python 3.11 和 3.12 上执行标准安装和测试。以具体提交的 Actions 结果为准；工作流文件存在不表示已经运行成功。

安全漏洞请按 [SECURITY.md](SECURITY.md) 报告。
