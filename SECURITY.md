# 安全问题报告

如果问题可能导致越权读取、覆盖已有文件、执行非预期命令或泄露私人信息，请优先使用仓库 **Security → Report a vulnerability** 的私密报告入口。若该入口尚未启用，可先提交一个不含复现细节的 Issue，请维护者提供私密联系方式。不要在公开 Issue 或日志里附上私人媒体、完整草稿、账号、令牌或可识别的用户目录。

报告请包含版本或提交号、macOS/Python/FFmpeg 版本、预期与实际行为，以及使用合成素材的最小复现。敏感复现代码仅放在双方确认的私密渠道。项目不承诺固定响应时限；请优先在最新发布版本复核。

## 使用边界

此服务以本地 stdio 运行，具备当前进程获准的文件访问能力。应由可信的本地客户端启动，不应未经额外隔离和鉴权直接暴露为公共网络服务。服务不上传媒体；客户端如何处理工具返回的路径、字幕和元数据，仍取决于客户端及模型服务的配置。

媒体探测与处理使用本地 FFmpeg。处理结果写入新文件，托管修订使用新目录；发布会向指定剪映草稿库追加项目，并备份首页索引。实际发布要求剪映完全退出。不要把未经检查的路径、原生草稿或效果缓存当成可信输入。

剪映草稿结构是实验性兼容实现。加密草稿、账户权益验证和任意现有项目重写不在支持范围内。应用版本差异和未经原生验证的功能，应作为兼容性限制如实记录，而不是当作已获安全保证。
